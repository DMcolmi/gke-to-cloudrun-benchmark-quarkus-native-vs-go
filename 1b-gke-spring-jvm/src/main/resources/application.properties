server.port=8080
spring.application.name=device-api-spring-jvm

# Disable startup banner to reduce noise (mirrors quarkus.banner.enabled=false)
spring.main.banner-mode=off

# Enable virtual threads (Java 21 + Spring Boot 4 / Spring Framework 7)
spring.threads.virtual.enabled=true
